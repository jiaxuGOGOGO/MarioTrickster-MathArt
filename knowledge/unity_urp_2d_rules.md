# SESSION-059 Unity URP 2D Native Rules

This file is generated by `UnityURP2DEvolutionBridge` and stores distilled engineering rules for Unity URP 2D secondary textures plus XPBD-to-VAT export.

## UNITY-URP2D-001 — Unity URP 2D Secondary Textures manual

Treat `_NormalMap` as a first-class secondary texture on sprite imports so Light2D can read repository-baked normals without manual artist wiring.

- Constraint: `normal.png -> SecondaryTexture['_NormalMap']`

## UNITY-URP2D-002 — Unity Sprite Editor data provider API

Use editor-side Sprite data providers and `ISecondaryTextureDataProvider` for deterministic import automation instead of relying on manual inspector edits.

- Constraint: `secondary textures must be written by editor automation`

## UNITY-URP2D-003 — Dead Cells pipeline references

Export multi-channel 2D assets as a coherent bundle: albedo plus lighting helpers such as normal/depth/mask should survive together from the Python pipeline into the engine.

- Constraint: `bundle = albedo + auxiliary maps + metadata`

## UNITY-URP2D-004 — Macklin et al. 2016 XPBD

Run cloth simulation offline where compliance-stable XPBD can iterate freely, then cache the result for downstream engines instead of forcing Unity 2D to solve the cloth at runtime.

- Constraint: `cloth_runtime = offline_cache_replay`

## UNITY-URP2D-005 — VAT in Unity / SideFX workflow

Store one vertex per texel and one frame per scanline so mesh deformation can be replayed entirely in shader code with near-zero CPU animation overhead.

- Constraint: `VAT width = vertex count; VAT height = frame count`
