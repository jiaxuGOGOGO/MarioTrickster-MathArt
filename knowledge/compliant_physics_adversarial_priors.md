# Compliant Physics & Adversarial Motion Priors Knowledge Base

> Auto-generated by Layer 3 Evolution System (SESSION-035).
> Contains distilled rules from DeepMimic, AMP, and VPoser research.

## [physics_pd] hard_constraint (confidence: 0.95)

> PD controller tracking must use compliant mode by default. The physics layer is a FOLLOWER, not a LEADER. Formula: θ_final = (1-α)*θ_target + α*θ_physics where α is the compliance parameter (0=pure kinematic, 1=pure physics). Default α=0.6 for balanced tracking.

Source: DeepMimic (Peng et al., SIGGRAPH 2018)

Parameters:
  - `compliance_mode`: compliant_pd
  - `default_alpha`: 0.6
  - `formula`: θ_final = (1-α)*θ_target + α*θ_physics

## [physics_pd] soft_default (confidence: 0.90)

> Per-joint compliance should vary by anatomical role. Load-bearing joints (hips, knees, ankles) need tighter tracking (α=0.75-0.85) to maintain balance. Expressive joints (shoulders, elbows, wrists) can be looser (α=0.25-0.45) to preserve artistic intent. Spine is moderate (α=0.55-0.65) for postural stability.

Source: DeepMimic joint-importance analysis

Parameters:
  - `hip_compliance`: 0.75
  - `knee_compliance`: 0.85
  - `ankle_compliance`: 0.80
  - `shoulder_compliance`: 0.35
  - `elbow_compliance`: 0.25
  - `wrist_compliance`: 0.25
  - `spine_compliance`: 0.55

## [physics_pd] heuristic (confidence: 0.85)

> Virtual muscle torque must be clamped to prevent physics from overpowering the reference motion. Maximum torque = k_p * 1.5 per joint. Gravity compensation torque is additive but does not count toward the torque limit. This prevents the "four-headed monster" effect where physics distorts the original motion beyond recognition.

Source: DeepMimic torque limiting + project pain point analysis

Parameters:
  - `max_torque_multiplier`: 1.5
  - `gravity_compensation`: additive_only

## [motion_evaluation] hard_constraint (confidence: 0.95)

> Layer 3 fitness evaluation MUST include AMP-style discriminator scoring alongside hand-written metrics. The discriminator provides a learned "does this look real?" signal that catches failure modes invisible to heuristic scoring. Weight: 30% of overall fitness.

Source: AMP (Peng et al., SIGGRAPH 2021)

Parameters:
  - `amp_weight_in_fitness`: 0.30
  - `reward_mode`: lsgan
  - `gradient_penalty_weight`: 10.0

## [motion_evaluation] soft_default (confidence: 0.85)

> AMP discriminator must use LSGAN formulation (not vanilla GAN) for training stability. Gradient penalty with weight 10.0 is critical — without it, the discriminator collapses within 50 training steps. Replay buffer with capacity 10000 prevents catastrophic forgetting.

Source: AMP training stability analysis

Parameters:
  - `reward_mode`: lsgan
  - `gradient_penalty_weight`: 10.0
  - `replay_buffer_capacity`: 10000

## [anatomy_prior] hard_constraint (confidence: 0.95)

> All pose mutations in the evolution system MUST go through VPoser-style latent space projection. Mutating directly in joint-angle space produces anatomically impossible poses (hyperextended knees, twisted spines). Latent space mutation guarantees every output is on the human pose manifold.

Source: VPoser (Ghorbani et al., CVPR 2019)

Parameters:
  - `mutation_space`: latent
  - `latent_dim`: 32
  - `decode_after_mutate`: always

## [anatomy_prior] soft_default (confidence: 0.90)

> VPoser naturalness score (Mahalanobis distance from latent origin) should contribute 10% to overall fitness. Poses with naturalness < 0.3 are likely anatomically implausible and should be penalized. The score naturally ranges [0, 1] where 1.0 = perfectly natural (at the latent origin).

Source: VPoser naturalness metric

Parameters:
  - `vposer_weight_in_fitness`: 0.10
  - `naturalness_threshold`: 0.30
  - `score_range`: [0.0, 1.0]

## [pipeline_bridge] hard_constraint (confidence: 0.90)

> Layer 3 evaluation results MUST automatically feed into the next produce_character_pack() call via the convergence bridge (LAYER3_CONVERGENCE_BRIDGE.json). The bridge persists: physics_stiffness, physics_damping, compliance_alpha, biomechanics_zmp_strength, amp_style_reward, vposer_naturalness, combined_fitness, archetype, and cycle_id.

Source: SESSION-035 Gap #3 fix

Parameters:
  - `bridge_file`: LAYER3_CONVERGENCE_BRIDGE.json
  - `auto_load`: true
  - `override_defaults`: true
