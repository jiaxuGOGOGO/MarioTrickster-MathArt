# Pipeline Contract Knowledge Base

> Auto-generated by SESSION-040 ContractEvolutionBridge. Updated 2026-04-16.

## Hard Constraints

1. **UMR_Context Immutability:** The pipeline context MUST be a frozen dataclass. Any mutation attempt MUST raise `FrozenInstanceError`. The `context_hash` is computed deterministically from character spec + random seed + convergence bridge parameters.

2. **PipelineContractError Fail-Fast:** Any attempt to bypass the UMR trunk (direct `legacy_pose` calls, unregistered states, wrong pipeline order) MUST trigger an immediate `PipelineContractError`. No backward compatibility for legacy paths.

3. **SHA-256 Hash Determinism:** Given the same `UMR_Context` (same random seed, same character spec, same convergence bridge), the pipeline MUST produce byte-identical output. The `UMR_Auditor` computes frame-level, state-level, and pipeline-level SHA-256 hashes that MUST be reproducible across runs.

4. **Contact Flicker Prohibition:** Contact tag toggle rate within a single state clip MUST NOT exceed 50%. The `ContactFlickerDetector` enforces this constraint. Violations indicate physics instability or incorrect contact inference.

## Heuristics

5. **Golden Master Regression:** The `.umr_manifest.json` file contains the `ManifestSeal` with all hash levels. CI pipelines SHOULD compare new runs against the golden master using `ManifestSeal.verify_against()`. Any hash drift indicates a code change that affects core logic.

6. **Contract Evolution Fitness:** In Layer 3 physics evolution, contract compliance contributes +0.1 fitness bonus for full compliance and -0.2 penalty for violations. This ensures evolution never optimizes toward contract-breaking solutions.

7. **Hash Stability Streak:** The `ContractEvolutionBridge` tracks consecutive generations with stable pipeline hashes. A streak of 5+ generations indicates convergence and triggers knowledge distillation of the stable configuration.

## Soft Defaults

8. **Pipeline Order:** The default pipeline order is `[phase_driven_generator, physics_projector, biomechanics_projector, render_config]`. Custom orders are allowed but MUST be registered with `PipelineContractGuard` before execution.

9. **State Coverage:** The default required states are `[idle, walk, run, jump, fall, hit]`. Additional states can be registered but MUST have corresponding phase-driven generators.

10. **Audit Trail:** Every pipeline run SHOULD produce an audit trail in the manifest. The trail includes node execution order, timing, and any warnings. This is informational and does not block execution.
