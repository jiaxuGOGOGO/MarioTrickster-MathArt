# Industrial Rendering & Motion Matching Knowledge Base

> Auto-generated by SESSION-034 research protocol.
> Contains distilled rules from Motion Matching (Clavet GDC 2016), Dead Cells (GDC 2018), and Guilty Gear Xrd (GDC 2015).

## [motion_matching] hard_constraint (confidence: 0.95)

> Feature vectors MUST include foot contact labels. Contact weight should be 1.5× other features to prevent foot skating. The 59-dimensional schema is: pose(12) + velocity(12) + trajectory(12) + contact(6) + phase(6) + silhouette(5) + trajectory_velocity(6).

Source: Clavet GDC 2016 + SESSION-034 implementation
Parameters:
  - `total_dims`: 59
  - `contact_weight`: 1.5
  - `normalization`: per-column (mean=0, std=1)

## [motion_matching] soft_default (confidence: 0.90)

> Per-column normalization (mean=0, std=1) is essential for feature-vector matching. Without normalization, high-variance features (e.g., trajectory) dominate the distance metric and low-variance features (e.g., contact labels) are ignored.

Source: Clavet GDC 2016
Parameters:
  - `normalization_method`: z-score per column
  - `distance_metric`: weighted Euclidean

## [rendering_pipeline] hard_constraint (confidence: 0.95)

> At 32×32 pixels, anti-aliasing is POISON. Use hard binary SDF threshold (dist < 0 = filled, dist >= 0 = empty). No smoothstep, no bilinear filtering, no sub-pixel blending. This is the Dead Cells lesson.

Source: Dead Cells GDC 2018
Parameters:
  - `threshold`: hard binary (dist < 0)
  - `anti_aliasing`: disabled
  - `target_resolution`: 32×32

## [rendering_pipeline] soft_default (confidence: 0.88)

> Pseudo-normal maps from SDF gradients provide volume cues even at 32×32. Compute nx = dSDF/dx, ny = dSDF/dy, nz = sqrt(1 - nx² - ny²). Apply 2-band cel shading: lit (ndotl > 0.25) and shadow (ndotl < -0.15).

Source: Dead Cells GDC 2018
Parameters:
  - `normal_source`: SDF gradient finite differences
  - `shading_bands`: 2 (lit + shadow)
  - `dithering`: none

## [rendering_pipeline] heuristic (confidence: 0.85)

> OKLAB color space for warm highlight / cool shadow. Shift highlight toward warm (a+0.02, b+0.03) and shadow toward cool (a-0.02, b-0.03). This creates natural-looking volume without explicit lighting.

Source: Dead Cells GDC 2018 + OKLAB perceptual uniformity
Parameters:
  - `color_space`: OKLAB
  - `warm_shift`: (a+0.02, b+0.03)
  - `cool_shift`: (a-0.02, b-0.03)

## [frame_scheduling] hard_constraint (confidence: 0.92)

> Hold frames at key animation phases: Contact (2 frames), Impact (3 frames), Apex (2 frames), Landing (2 frames). Use stepped interpolation (snap, not blend). This is the Guilty Gear Xrd limited animation technique.

Source: Guilty Gear Xrd GDC 2015
Parameters:
  - `contact_hold`: 2 frames
  - `impact_hold`: 3 frames
  - `apex_hold`: 2 frames
  - `landing_hold`: 2 frames
  - `interpolation`: stepped (no blending)

## [frame_scheduling] soft_default (confidence: 0.88)

> Extreme squash & stretch per phase masks physics imperfections with visual impact. Contact: squash_y=0.88. Impact: squash_y=0.75. Apex: stretch_y=1.18. Landing: squash_y=0.85. Volume is preserved by inverse X scaling.

Source: Guilty Gear Xrd GDC 2015
Parameters:
  - `contact_squash_y`: 0.88
  - `impact_squash_y`: 0.75
  - `apex_stretch_y`: 1.18
  - `landing_squash_y`: 0.85
  - `volume_preservation`: inverse X scaling

## [silhouette_quality] heuristic (confidence: 0.85)

> At 32×32, silhouette readability is the single most important visual metric. Measure extremity spread (hand/foot distance from center), bounding box aspect ratio, and outline pixel count. Boost outline thickness on impact frames.

Source: Dead Cells GDC 2018 + Guilty Gear Xrd GDC 2015
Parameters:
  - `outline_boost_on_impact`: +1 iteration
  - `silhouette_metrics`: extremity_spread, bbox_aspect, outline_pixels
  - `target_resolution`: 32×32
